
np.random.seed(3<<17)
# np.random.seed(70)
# np.random.seed(0)

ratio_sample_size = 10000
D = 20

xpoints = [d for d in range(1, D + 1)]
ypoints = []

for d in range(1, D + 1):
    ratio = volume_ratio(d)
    V = ratio * (2**d)
    ypoints.append(V)

plt.plot(xpoints, ypoints)
plt.show()
    

